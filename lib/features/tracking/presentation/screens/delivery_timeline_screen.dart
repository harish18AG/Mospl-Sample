import 'package:flutter/material.dart';
import '../../../../core/widgets/app_scaffold.dart';

class DeliveryTimelineScreen extends StatelessWidget {
  const DeliveryTimelineScreen({super.key});
  @override
  Widget build(BuildContext context) => AppScaffold(title: 'Delivery Timeline', body: const Center(child: Text('Delivery Timeline')));
}
