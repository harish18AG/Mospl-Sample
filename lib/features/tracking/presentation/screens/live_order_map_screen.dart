import 'package:flutter/material.dart';
import '../../../../core/widgets/app_scaffold.dart';

class LiveOrderMapScreen extends StatelessWidget {
  const LiveOrderMapScreen({super.key});
  @override
  Widget build(BuildContext context) => AppScaffold(title: 'Live Order Map', body: const Center(child: Text('Live Order Map')));
}
