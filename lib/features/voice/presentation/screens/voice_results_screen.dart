import 'package:flutter/material.dart';
import '../../../../core/widgets/app_scaffold.dart';

class VoiceResultsScreen extends StatelessWidget {
  const VoiceResultsScreen({super.key});
  @override
  Widget build(BuildContext context) => AppScaffold(title: 'Voice Results', body: const Center(child: Text('Voice Results')));
}
