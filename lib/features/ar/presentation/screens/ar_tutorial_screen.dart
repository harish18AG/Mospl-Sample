import 'package:flutter/material.dart';
import '../../../../core/widgets/app_scaffold.dart';

class ArTutorialScreen extends StatelessWidget {
  const ArTutorialScreen({super.key});
  @override
  Widget build(BuildContext context) => AppScaffold(title: 'Ar Tutorial', body: const Center(child: Text('Ar Tutorial')));
}
