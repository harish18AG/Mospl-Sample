import 'package:flutter/material.dart';
import '../../../../core/widgets/app_scaffold.dart';

class ArBagPreviewScreen extends StatelessWidget {
  const ArBagPreviewScreen({super.key});
  @override
  Widget build(BuildContext context) => AppScaffold(title: 'Ar Bag Preview', body: const Center(child: Text('Ar Bag Preview')));
}
