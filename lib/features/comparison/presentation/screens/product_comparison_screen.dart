import 'package:flutter/material.dart';
import '../../../../core/widgets/app_scaffold.dart';

class ProductComparisonScreen extends StatelessWidget {
  const ProductComparisonScreen({super.key});
  @override
  Widget build(BuildContext context) => AppScaffold(title: 'Product Comparison', body: const Center(child: Text('Product Comparison')));
}
