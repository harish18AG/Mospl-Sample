import 'package:flutter/material.dart';
import '../../../../core/widgets/app_scaffold.dart';

class ComparisonResultScreen extends StatelessWidget {
  const ComparisonResultScreen({super.key});
  @override
  Widget build(BuildContext context) => AppScaffold(title: 'Comparison Result', body: const Center(child: Text('Comparison Result')));
}
