import 'package:flutter/material.dart';
import '../../../../core/widgets/app_scaffold.dart';

class FlashSaleHomeScreen extends StatelessWidget {
  const FlashSaleHomeScreen({super.key});
  @override
  Widget build(BuildContext context) => AppScaffold(title: 'Flash Sale Home', body: const Center(child: Text('Flash Sale Home')));
}
