import 'package:flutter/material.dart';
import '../../../../core/widgets/app_scaffold.dart';

class FlashSaleDetailScreen extends StatelessWidget {
  const FlashSaleDetailScreen({super.key});
  @override
  Widget build(BuildContext context) => AppScaffold(title: 'Flash Sale Detail', body: const Center(child: Text('Flash Sale Detail')));
}
